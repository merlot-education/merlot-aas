user  nginx;

events {
    worker_connections   1000;
}
http {
        server {
              listen 4000;

              location / {
                proxy_pass http://aas:9000;
              }
        }
        server {
              listen 5000;

              location / {
                proxy_pass http://demo:8990;
              }
        }
}